- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- apt-get:
  - rake
  - fonts-hack-ttf
  - global
  - npm
  - tmux
  - ack-grep
  - silversearcher-ag
  - exuberant-ctags
  - build-essential
  - cmake
  - python3-dev
  - xpad
  - imagemagick
  - dconf-cli 

- link:
    ~/.dotfiles: ''
    ~/.spacemacs: spacemacs
    ~/.gitconfig: gitconfig
    ~/.oh-my-zsh: oh-my-zsh/
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.zshenv: zshenv
    ~/.fzf: fzf/
  
- shell:
  # Fix any broken packages.
  - [sudo apt-get install -f, Fixing any broken packages.]
  - [sudo npm install -g diff-so-fancy, Installing diff-so-fancy]
  - [sudo npm install -g tern, Installing tern]
